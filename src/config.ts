import "dotenv/config";

const ENV = process.env;

const config = {
  RESPONSE_CONTENT_TYPE: ENV.RESPONSE_CONTENT_TYPE || "application/json",
  DEFAULT_PORT: Number(ENV.PORT || 3300),
  SERVICE_NAME: ENV.SERVICE_NAME || "@sha3/polymarket-crypto-prediction",
  EXECUTION_MODE: ENV.EXECUTION_MODE === "real" ? "real" : "paper",
  LLM_LOG_DIRECTORY: ENV.LLM_LOG_DIRECTORY || "runtime/llm",
  LLM_PROMPT_MAX_CHARS: Number(ENV.LLM_PROMPT_MAX_CHARS || 12_000),
  LLM_PROMPT_RECENT_EVENT_LIMIT: Number(ENV.LLM_PROMPT_RECENT_EVENT_LIMIT || 30),
  SNAPSHOT_INTERVAL_MS: Number(ENV.SNAPSHOT_INTERVAL_MS || 500),
  CROSS_THRESHOLD: Number(ENV.CROSS_THRESHOLD || 0.02),
  MARKET_COOLDOWN_MS: Number(ENV.MARKET_COOLDOWN_MS || 5_000),
  TRIGGER_CONFIRMATION_DELAY_MS: Number(ENV.TRIGGER_CONFIRMATION_DELAY_MS || 1_500),
  MIN_TRIGGER_DISTANCE_FROM_HALF: Number(ENV.MIN_TRIGGER_DISTANCE_FROM_HALF || 0.01),
  MIN_TRIGGER_SPOT_MOMENTUM: Number(ENV.MIN_TRIGGER_SPOT_MOMENTUM || 0.0005),
  MIN_RESEARCH_MARKET_QUALITY: Number(ENV.MIN_RESEARCH_MARKET_QUALITY || 0.75),
  MIN_WEAK_BREADTH_STRENGTH_FOR_PREDICTION: Number(ENV.MIN_WEAK_BREADTH_STRENGTH_FOR_PREDICTION || 0.05),
  PREDICTION_HORIZON_MS: Number(ENV.PREDICTION_HORIZON_MS || 30_000),
  SHORT_HISTORY_SECONDS: Number(ENV.SHORT_HISTORY_SECONDS || 120),
  LONG_HISTORY_SECONDS: Number(ENV.LONG_HISTORY_SECONDS || 600),
  MAX_PREDICTION_HISTORY_PER_MARKET: Number(ENV.MAX_PREDICTION_HISTORY_PER_MARKET || 200),
  MAX_PREDICTION_QUERY_LIMIT: Number(ENV.MAX_PREDICTION_QUERY_LIMIT || 200),
  TOKEN_MAX_AGE_MS: Number(ENV.TOKEN_MAX_AGE_MS || 3_000),
  SPOT_MAX_AGE_MS: Number(ENV.SPOT_MAX_AGE_MS || 3_000),
  CHAINLINK_MAX_AGE_MS: Number(ENV.CHAINLINK_MAX_AGE_MS || 5_000),
  ENSEMBLE_MEDIUM_CONFIDENCE_THRESHOLD: Number(ENV.ENSEMBLE_MEDIUM_CONFIDENCE_THRESHOLD || 0.6),
  ENSEMBLE_HIGH_CONFIDENCE_THRESHOLD: Number(ENV.ENSEMBLE_HIGH_CONFIDENCE_THRESHOLD || 0.72),
  ENSEMBLE_SCORE_ESCALATION_THRESHOLD: Number(ENV.ENSEMBLE_SCORE_ESCALATION_THRESHOLD || 0.15),
  STRATEGY_ROLLING_WINDOW_SECONDS: Number(ENV.STRATEGY_ROLLING_WINDOW_SECONDS || 7_200),
  COMBO_ROLLING_WINDOW_SECONDS: Number(ENV.COMBO_ROLLING_WINDOW_SECONDS || 7_200),
  COMBO_TOP_STRATEGIES_FOR_PAIRS: Number(ENV.COMBO_TOP_STRATEGIES_FOR_PAIRS || 8),
  COMBO_TOP_STRATEGIES_FOR_TRIOS: Number(ENV.COMBO_TOP_STRATEGIES_FOR_TRIOS || 5),
  COMBO_MAX_CANDIDATE_STRATEGIES: Number(ENV.COMBO_MAX_CANDIDATE_STRATEGIES || 6),
  MIN_STRATEGY_SCORE_FOR_COMBO: Number(ENV.MIN_STRATEGY_SCORE_FOR_COMBO || 0.04),
  MIN_STRATEGY_CONFIDENCE_FOR_COMBO: Number(ENV.MIN_STRATEGY_CONFIDENCE_FOR_COMBO || 0.54),
  MIN_COMBO_SAMPLES_PAIR: Number(ENV.MIN_COMBO_SAMPLES_PAIR || 8),
  MIN_COMBO_SAMPLES_TRIO: Number(ENV.MIN_COMBO_SAMPLES_TRIO || 10),
  MIN_COMBO_EXECUTION_SAMPLES_PAIR: Number(ENV.MIN_COMBO_EXECUTION_SAMPLES_PAIR || 12),
  MIN_COMBO_EXECUTION_SAMPLES_TRIO: Number(ENV.MIN_COMBO_EXECUTION_SAMPLES_TRIO || 16),
  MIN_COMBO_LIFT_PNL: Number(ENV.MIN_COMBO_LIFT_PNL || 0),
  MIN_COMBO_LIFT_HIT: Number(ENV.MIN_COMBO_LIFT_HIT || 0.02),
  MIN_COMBO_SCORE_FOR_BOOST: Number(ENV.MIN_COMBO_SCORE_FOR_BOOST || 0.58),
  MIN_COMBO_EXECUTION_SCORE: Number(ENV.MIN_COMBO_EXECUTION_SCORE || 0.68),
  MIN_COMBO_AGREEMENT_PURITY_FOR_PENALTY: Number(ENV.MIN_COMBO_AGREEMENT_PURITY_FOR_PENALTY || 0.72),
  MAX_PAIR_BOOST_ABS: Number(ENV.MAX_PAIR_BOOST_ABS || 0.06),
  MAX_TRIO_BOOST_ABS: Number(ENV.MAX_TRIO_BOOST_ABS || 0.1),
  MAX_TOTAL_COMBO_BOOST_ABS: Number(ENV.MAX_TOTAL_COMBO_BOOST_ABS || 0.18),
  MAX_TOTAL_COMBO_CONFIDENCE_PENALTY: Number(ENV.MAX_TOTAL_COMBO_CONFIDENCE_PENALTY || 0.12),
  ENABLE_COMBO_BOOST: ENV.ENABLE_COMBO_BOOST !== "false",
  DASHBOARD_POLL_INTERVAL_MS: Number(ENV.DASHBOARD_POLL_INTERVAL_MS || 2_000),
  CROSS_ASSET_LOOKBACK_MS: Number(ENV.CROSS_ASSET_LOOKBACK_MS || 15_000),
  MARKET_SCORE_WINDOW_SECONDS: Number(ENV.MARKET_SCORE_WINDOW_SECONDS || 7_200),
  MIN_MARKET_TRADES_FOR_SCORING: Number(ENV.MIN_MARKET_TRADES_FOR_SCORING || 3),
  MIN_MARKET_SCORE_FOR_ENTRY: Number(ENV.MIN_MARKET_SCORE_FOR_ENTRY || 0.65),
  MIN_MARKET_PREDICTIONS_BEFORE_ENTRY: Number(ENV.MIN_MARKET_PREDICTIONS_BEFORE_ENTRY || 4),
  MIN_RESEARCH_PREDICTIONS_FOR_BOOTSTRAP: Number(ENV.MIN_RESEARCH_PREDICTIONS_FOR_BOOTSTRAP || 6),
  ENTRY_TARGET_PRICE: Number(ENV.ENTRY_TARGET_PRICE || 0.5),
  ENTRY_BAND_HALF_WIDTH: Number(ENV.ENTRY_BAND_HALF_WIDTH || 0.01),
  MIN_ORDER_USD: Number(ENV.MIN_ORDER_USD || 1),
  MIN_ORDER_SHARES: Number(ENV.MIN_ORDER_SHARES || 5),
  TAKE_PROFIT_DELTA: Number(ENV.TAKE_PROFIT_DELTA || 0.12),
  STOP_LOSS_DELTA: Number(ENV.STOP_LOSS_DELTA || 0.08),
  MIN_ENTRY_CONFIDENCE: Number(ENV.MIN_ENTRY_CONFIDENCE || 0.72),
  MIN_MARKET_QUALITY_FOR_ENTRY: Number(ENV.MIN_MARKET_QUALITY_FOR_ENTRY || 0.82),
  MIN_SPREAD_FOR_MAKER: Number(ENV.MIN_SPREAD_FOR_MAKER || 0.02),
  MAX_SPREAD_FOR_ENTRY: Number(ENV.MAX_SPREAD_FOR_ENTRY || 0.05),
  MAKER_ENTRY_TIMEOUT_MS: Number(ENV.MAKER_ENTRY_TIMEOUT_MS || 10_000),
  MAKER_EXIT_TIMEOUT_MS: Number(ENV.MAKER_EXIT_TIMEOUT_MS || 5_000),
  MIN_DEPTH_FOR_MAKER: Number(ENV.MIN_DEPTH_FOR_MAKER || 25),
  MAKER_DRIFT_LIMIT: Number(ENV.MAKER_DRIFT_LIMIT || 0.03),
  TAKER_URGENCY_THRESHOLD: Number(ENV.TAKER_URGENCY_THRESHOLD || 0.65),
  LOW_DEPTH_SLIPPAGE_PROXY: Number(ENV.LOW_DEPTH_SLIPPAGE_PROXY || 0.01),
  MAX_OPEN_POSITIONS_GLOBAL: Number(ENV.MAX_OPEN_POSITIONS_GLOBAL || 3),
  REAL_BALANCE_REFRESH_MS: Number(ENV.REAL_BALANCE_REFRESH_MS || 30_000),
  POLYMARKET_PRIVATE_KEY: ENV.POLYMARKET_PRIVATE_KEY || "",
  POLYMARKET_FUNDER_ADDRESS: ENV.POLYMARKET_FUNDER_ADDRESS || "",
  POLYMARKET_SIGNATURE_TYPE: ENV.POLYMARKET_SIGNATURE_TYPE || "",
  POLYMARKET_MAX_ALLOWED_SLIPPAGE: ENV.POLYMARKET_MAX_ALLOWED_SLIPPAGE || "",
  CROSS_ASSET_BREADTH_MOVE_THRESHOLD: Number(ENV.CROSS_ASSET_BREADTH_MOVE_THRESHOLD || 0.015),
  CROSS_ASSET_BREADTH_MIN_PARTICIPATION: Number(ENV.CROSS_ASSET_BREADTH_MIN_PARTICIPATION || 0.75),
  CROSS_ASSET_BREADTH_MIN_STRENGTH: Number(ENV.CROSS_ASSET_BREADTH_MIN_STRENGTH || 0.55),
  CROSS_ASSET_LAGGARD_THRESHOLD: Number(ENV.CROSS_ASSET_LAGGARD_THRESHOLD || 0.35),
} as const;

export default config;
